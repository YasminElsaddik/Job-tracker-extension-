// content.js
// This file allows the extension to interact with job listing pages.
// It doesn't need to do anything active — the popup handles the extraction.
console.log("Job Tracker content script loaded.");